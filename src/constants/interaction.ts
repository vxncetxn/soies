/** Default hold duration before onLongPress fires (Pressable-based surfaces). */
export const LONG_PRESS_MIN_DURATION_MS = 500;

/** Movement tolerance before the scroll-indicator's RN long-press timer cancels. */
export const LONG_PRESS_MAX_DISTANCE_PX = 1000;

/** Shorter hold for scroll-indicator scrub (long-press then pan). */
export const SCROLL_INDICATOR_LONG_PRESS_MIN_DURATION_MS = 250;
