# Scribble / Ink adversarial review

**Baseline:** HEAD `0f53f33` + uncommitted worktree

Engineering-lead review prioritized by drawing performance, crash/data-loss safety, then maintainability and plan compliance.

| Metric                    | Value  |
| ------------------------- | ------ |
| Critical runtime blockers | 0 open |
| Additional high risks     | 0 open |
| Static check failure      | 0      |
| Compiler coverage         | 63/63  |

## Verdict: remediation complete; native device QA remains

The implementation now removes full-document bridge/PNG work from interaction paths, batches each eraser drag into one native history transaction, captures JSON/PNG from one immutable revision, and performs encoding/file I/O off the UI thread. Draft and durable file ownership are explicit and rollback-safe. Static release gates pass; the remaining gate is rebuilt iOS/Android device QA, including the Android density matrix.

### Closed release blockers

- Pen-up only marks native drawing state; export occurs on explicit Save.
- Native erasing does bounded segment hit-testing and one history transaction per drag.
- Snapshot rejects active gestures, freezes one revision, and moves encoding/file I/O to a serial worker.
- Android Promise result events are non-coalescible.
- Failed prefetch/export keeps Scribble open with Retry and cleans the failed temporary file.

### What is sound

- Migration V2 and `schema_version` advance atomically.
- Insert/select/gallery annotation wiring is present.
- The pager bounds native canvases to active ±1.
- Android normal pen movement draws incrementally without React state.
- Native teardown cancels replay callbacks and releases primary resources.
- Ink remains separate from Print photo bytes.

---

## Runtime, performance, and reliability findings

| Location                                                                   | Severity | Finding                                                                                                       | Concrete impact                                                                                                                                                                                                               | Required remediation                                                                                                                                        | Addressed | Implementer's Remarks                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SignatureInkViewManager.kt:403–423`                                       | Critical | Android result events coalesce                                                                                | The generic result event keeps React Native’s default `canCoalesce() = true` and key 0. Concurrent `getStrokeData` and `toFile` responses can collapse into one event, leaving the other Promise pending until unmount.       | Make request/result events non-coalescible. Use separate event classes; only replay progress should coalesce.                                               | Yes       | Fixed: `SignatureInkResultEvent` with `canCoalesce() = false` + per-requestId key; replay progress still coalesces.                                                                                                                                                                                                                                                                                                         |
| `ArtefactInkCanvas.tsx:246–258`; `SignatureCanvasView.kt:322–370, 631–650` | Critical | Android eraser mixes dp and px                                                                                | React Native converts responder `locationX`/`locationY` to dp, while Android stores `MotionEvent` x/y as physical pixels. On 2×/3× devices, erasing is offset or misses.                                                      | Move hit-testing into native code or normalize the versioned wire format to dp/normalized coordinates.                                                      | Yes       | Native `eraseStrokeNear` (dp in, px hit-test) + Android wire format now emits/accepts dp. Requires native rebuild.                                                                                                                                                                                                                                                                                                          |
| `ArtefactInkCanvas.tsx:85–109, 185–205, 299–305`                           | Critical | Whole-document work in interaction paths                                                                      | Every pen-up serializes all points and exports a full PNG. Every eraser frame scans all points, serializes the remaining document, reconstructs the native canvas, encodes/writes a PNG, then decodes it through expo-image.  | Mark the canvas dirty on pen-up. Implement native stroke erasing as one gesture/history transaction. Export only at Save or gesture end, off the UI thread. | Yes       | Native begin/move/end eraser commands commit one Undo/Redo transaction. Android history retains only removed stroke references/indices rather than whole-document copies. Cached bounds (Android) and PencilKit render bounds (iOS) reject unrelated strokes before detailed path checks; Android measures point-to-segment distance. Export occurs only on Save, with composition, compression, and I/O on serial workers. |
| `ArtefactInkCanvas.tsx:185–240`                                            | Critical | Stroke JSON and PNG are not one snapshot                                                                      | Commit reads stroke data and exports PNG as separate native commands while drawing, Back, Undo, another Save, or a pending eraser RAF can intervene. `annotations` and the visible overlay can represent different revisions. | Expose one native snapshot command returning stroke data and a PNG from the same immutable drawing revision; lock all editing controls while it runs.       | Yes       | Native `snapshot` rejects active pen/eraser gestures and captures immutable drawing/bitmap state, source canvas dimensions, and stroke state on the UI thread before worker export. `scribbleSaving` and create-session busy state lock editing and hardware Back until settlement.                                                                                                                                         |
| `ArtefactInkCanvas.tsx:60–66, 216–234`                                     | High     | Operational failures become valid-looking empty Ink                                                           | All native errors are swallowed. A failed stroke read becomes an empty document; a failed export becomes an empty URI. Both screens leave Scribble as though Save succeeded.                                                  | Suppress only classified cancellation in preview work. Commit must reject atomically, keep Scribble open, and present Retry.                                | Yes       | `commit` rejects; Save Alert+Retry; Scribble stays open on failure.                                                                                                                                                                                                                                                                                                                                                         |
| `SignatureInkSurface.swift:1049–1057`; `ArtefactInkCanvas.tsx:207–240`     | High     | Save and Back violate history boundaries                                                                      | Save does not clear history. On iOS, Back reloads committed strokes through `setStrokeData`, which pushes the discarded drawing onto Undo; re-entering and pressing Undo can resurrect discarded Ink.                         | Separate reset-with-cleared-history from undoable replacement, and add `clearHistoryPreservingDrawing` after a successful snapshot.                         | Yes       | `replaceStrokeData` for Back/load; `clearHistory` after successful snapshot.                                                                                                                                                                                                                                                                                                                                                |
| `SignatureCanvasView.kt:694–751`; `SignatureInkSurface.swift:1060–1138`    | High     | Replay cancellation destroys undisplayed strokes                                                              | Replay clears/replaces the real drawing. Touch, Undo, or another replay cancels only the frame callback/display link, leaving the partial replay as the new source of truth.                                                  | Restore the final snapshot on interactive cancellation; reserve non-restoring cancellation for teardown and explicit Clear.                                 | Yes       | Interactive cancellation now restores `replaySnapshot` / `replayFinalDrawing` before the next touch or command can mutate the document.                                                                                                                                                                                                                                                                                     |
| `SignatureCanvasView.kt:48–50, 222–228, 583–593, 811–833`                  | High     | Android bitmap allocation pressure                                                                            | Color changes rebuild up to three mounted full-size ARGB canvases. Every export allocates another bitmap; old backing and temporary export bitmaps are not eagerly recycled.                                                  | Do not rebuild for pen-color changes, reuse same-sized bitmaps, recycle replaced/export bitmaps in `finally`, and remove continuous exports.                | Yes       | `penColor` no longer rebuilds; old/export bitmaps are recycled; continuous exports are removed. Save freezes one bitmap copy, performs crop/composite/encode on a serial worker, and recycles worker bitmaps in `finally`.                                                                                                                                                                                                  |
| `artefacts.ts:40–48`; `entries.ts:204–229`; `entriesCache.ts:3–25`         | High     | Display reads retain every stroke point                                                                       | Home/startup queries transfer annotations through SQLite, parse every point into JS objects, and retain them in the module cache even though display code consumes only `inkOverlayPath`.                                     | Project `hasInk`/overlay URI in display queries and load/parse annotations only when an editable canvas opens.                                              | Yes       | Entry and Gallery display queries now project SQL `has_ink` from the canonical nullable column and do not select `annotations`. Full bounded parsing happens only through `getArtefactInkDocument` when editing.                                                                                                                                                                                                            |
| `savePrintEntry.ts:44–53, 69–75`                                           | High     | Overlay failure orphans the current photo                                                                     | The image copy succeeds before the artefact is registered in `prepared`. If overlay copy fails, catch cleanup cannot see the current photo.                                                                                   | Register cleanup ownership immediately after each side effect; centralize Paper/Print preparation and failure injection tests.                              | Yes       | Print registers the photo immediately after copy. Paper registers the artefact ID before overlay copy, so even a partially-created destination is visible to cleanup. Both use rollback-safe overlay installation. Automated failure injection remains absent because the repository has no test runner.                                                                                                                    |
| `ArtefactInkCanvas.tsx:156–166, 192–196, 299–305`                          | Medium   | Preview files and image-cache entries grow without bound                                                      | Timestamped full-canvas PNGs are created after strokes and throughout erasing. Superseded files are never deleted and unique URIs churn memory/disk caches.                                                                   | Own one replaceable preview path per canvas session and remove it on replacement, durable copy, cancellation, and unmount.                                  | Yes       | A successful canvas commit transfers ownership to `useScribbleSession`; pager unmount no longer deletes draft-owned PNGs. Replacement and session teardown delete superseded temps. Failed/cancelled snapshots self-delete. Durable replacement uses sibling temp + backup + rollback instead of delete-before-copy.                                                                                                        |
| `ArtefactInkCanvas.tsx`; `useScribbleSession.tsx`                          | High     | Re-audit: Save accepted an unavailable PNG and canvas unmount could delete the draft's only committed overlay | `Image.prefetch` rejection/`false` was ignored, and pager virtualization could unmount the canvas that still owned the URI referenced by draft state. Entry Submit then failed later and farther from the cause.              | Make prefetch part of the commit contract and transfer successful temporary-file ownership above the virtualized page.                                      | Yes       | False or rejected prefetch now rejects Save and deletes the failed file. Successful commit transfers ownership to the create-level session, keyed by artefact ID; virtualized canvases no longer delete draft-owned overlays.                                                                                                                                                                                               |
| `useScribbleSession.tsx`; `CreateOverlay.tsx`                              | High     | Re-audit: hardware Back could unmount during Scribble Save                                                    | Scribble Save did not set the create-session busy flag and promise completion could update stale state or display an Alert after unmount.                                                                                     | Share the create busy lock and invalidate/clean late completions on unmount.                                                                                | Yes       | Scribble Save now sets `createSessionBusy`, blocking hardware Back. An alive flag plus generation invalidates late completions, cleans their file, suppresses stale Alert/state updates, and releases busy state on settlement/teardown.                                                                                                                                                                                    |
| `storage/files.ts`; `savePaperEntry.ts`                                    | High     | Re-audit: delete-before-copy and late Paper ownership were not failure-atomic                                 | Failed replacement could destroy the previous durable overlay; failed current Paper copy could leave output that cleanup did not own.                                                                                         | Install through a sibling temporary file with rollback, and register cleanup identity before each fallible copy.                                            | Yes       | Overlay replacement now copies to a unique sibling, moves any prior destination to a backup, installs the completed temp, and restores/retains the backup on failure. Paper registers its artefact ID before overlay work, so catch cleanup covers partial output.                                                                                                                                                          |

---

## Standards and code hygiene

Kept separate from plan compliance so clean implementation and correct product behavior do not mask each other.

| Classification | Location                                                       | Finding                                                                                                                                         | Addressed | Implementer's Remarks                                                                                                                                                                                                                      |
| -------------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Hard           | `ArtefactInkCanvas.tsx:60–66, 216–234`                         | The Save contract requires a stroke snapshot and prefetched PNG; converting every failure to empty output violates that contract.               | Yes       | Commit rejects; caller Alert+Retry.                                                                                                                                                                                                        |
| Hard           | `savePrintEntry.ts:35–36, 44–53`                               | The implementation’s own no-orphan cleanup invariant is false for the current artefact when overlay copying fails.                              | Yes       | Register photo before overlay copy.                                                                                                                                                                                                        |
| Judgment       | `data/ink.ts:47–75`                                            | Persisted points are asserted as `InkPoint[]` without finite numeric, width, count, or bounds validation before geometry/native use.            | Yes       | Version 2 validates document byte length, canvas dimensions, stroke/point/total counts, coordinate/timestamp/pressure/angle/width bounds, strict hex colors, and rejects the whole malformed document instead of silently dropping points. |
| Judgment       | `CreatePaperScreen.tsx:60–179`; `CreatePrintScreen.tsx:65–210` | Tool state, canvas lookup, Save/Back orchestration, and tool-strip construction are duplicated and already expose two independent drift points. | Yes       | Extracted `useScribbleSession`.                                                                                                                                                                                                            |
| Judgment       | `storage/files.ts:3`; `data/ink.ts:37–40`                      | Low-level storage imports filename policy from data while data save modules import storage, reversing the intended dependency direction.        | Yes       | `inkOverlayFileName` now lives in `storage/files`.                                                                                                                                                                                         |
| Quality gate   | `ArtefactInkCanvas.tsx:260–264`                                | Oxlint rejects synchronous state updates inside the eraser-mode effect with React Compiler `EffectSetState`.                                    | Yes       | Eraser PNG effect removed with native erase path.                                                                                                                                                                                          |
| Quality gate   | Vendored `src`, `lib/module`, and `lib/typescript` outputs     | Re-audit found stale generated outputs/source maps after the native interface changed.                                                          | Yes       | Source, runtime JS, and declarations now agree on eraser gesture commands and snapshot dimensions. Source maps for changed generated files were removed instead of shipping mappings to obsolete source.                                   |

---

## Plan compliance

| Result             | Requirement                        | Spec evidence                                                    | Assessment                                                                                                                                                                                  | Addressed | Implementer's Remarks                                                                                                                                                                                                                |
| ------------------ | ---------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Wrong              | Android eraser                     | Plan:78–85, 136–142                                              | The dp/px mismatch means the required Android eraser is not correctly implemented on normal-density devices.                                                                                | Yes       | Native erase accepts dp, converts once for pixel hit-testing, measures point-to-segment distance, and batches a complete drag into one undo/redo transaction. Density-matrix device verification remains a release gate.             |
| Wrong              | Save/Back history                  | Plan:33–37, 112–113, 133                                         | Save does not clear prior history; iOS Back can make discarded strokes undo-restorable.                                                                                                     | Yes       | `clearHistory` / `replaceStrokeData`.                                                                                                                                                                                                |
| Wrong              | Atomic Save                        | Plan:37, 82, 112                                                 | Errors are swallowed and the JSON/PNG pair is captured by separate mutable commands.                                                                                                        | Yes       | One native snapshot revision returns strokes, PNG, and canvas dimensions; active gestures reject. False/rejected PNG prefetch rejects commit, cleans the file, keeps Scribble open, and offers Retry.                                |
| Partial            | Save flow                          | Plan:112                                                         | The plan prefers staying in Scribble for continuous drawing; both screens exit immediately after Save.                                                                                      | No        | **Skipped by request** — deliberate UX.                                                                                                                                                                                              |
| Missing            | Repository update seam             | Plan:89–93                                                       | Insert/select/map exist, but there is no durable annotations update operation.                                                                                                              | Yes       | Added `updateArtefactAnnotations` + `getArtefactInkDocument`.                                                                                                                                                                        |
| Wrong              | Overlay lifecycle                  | Plan:92–93, 121                                                  | Normal drawing generates unmanaged temporary PNGs, and failed Print preparation can orphan a copied photo.                                                                                  | Yes       | Create-session ownership survives pager virtualization, cleans replacement/teardown, and transfers only after successful commit. Paper/Print register cleanup before fallible overlay work; durable replacement has backup rollback. |
| Architecture drift | Default/Type display               | Plan:115, 119–120                                                | Default/Type retain live native canvases and suppress the PNG cache. ADR-0008 documents this newer choice, so it is deliberate, but it increases native memory and widens the fork surface. | No        | **Skipped by request** — deliberate ADR-0008.                                                                                                                                                                                        |
| Pass by inspection | Current artefact + pager isolation | `CreatePaperScreen.tsx:133–163`; `CreatePrintScreen.tsx:148–177` | No evidence-backed wrong-artefact save or enabled pager interaction was found; active ID lookup and Scribble scroll locks are present.                                                      | N/A       | Still holds; Save lock also disables pager scroll.                                                                                                                                                                                   |

---

## Verification evidence

| Gate                  | Result               | Evidence                                                                                                                                                                                                                                                                                  |
| --------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting            | Pass                 | Full `oxfmt --check` after remediation.                                                                                                                                                                                                                                                   |
| TypeScript            | Pass                 | `tsc --noEmit` completed successfully after fixes.                                                                                                                                                                                                                                        |
| Oxlint                | Pass (touched files) | No React Compiler errors on rewritten canvas/hook.                                                                                                                                                                                                                                        |
| React Compiler health | Pass                 | 63/63 components compiled.                                                                                                                                                                                                                                                                |
| Whitespace            | Pass                 | `git diff --check`.                                                                                                                                                                                                                                                                       |
| Expo Doctor           | Inconclusive         | Not re-run; prior run was blocked by network access.                                                                                                                                                                                                                                      |
| Automated tests       | Absent               | Still no test/spec files; failure-injection tests not added.                                                                                                                                                                                                                              |
| Native rebuild        | Partial              | iOS and Android codegen pass. The iOS `SignatureInk` simulator target builds, and the physical-device Expo app launched and completed its Metro bundle. Android Gradle verification remains blocked because no Java runtime is installed. Android rebuild and device QA remain mandatory. |

### Static inspection cannot close the native gate

Android Fabric delivery, 1×/2×/3× erasing, orientation changes, PencilKit history handoff, memory stability, and cache cleanup still require instrumented device tests after rebuild.

---

## Recommended closure order

| Order | Workstream                    | Exit criterion                                                                                                                               |
| ----- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Redesign the hot path         | Native eraser; one gesture transaction; no per-frame bridge/PNG work; one immutable snapshot command; background encoding.                   |
| 2     | Make state transitions atomic | Disable tools/Back/Save while committing; flush pending eraser work; bind completion to artefact ID + generation; propagate errors.          |
| 3     | Normalize durable Ink         | Version canvas dimensions and normalized/dp coordinates; align timestamps, widths, pressure, and RGBA across iOS/Android.                    |
| 4     | Bound memory and storage      | Lazy-load annotations, remove unused iOS events, reuse/recycle Android bitmaps, use one owned preview path, atomically replace overlays.     |
| 5     | Harden persistence            | Register cleanup before each copy, validate PNG/document boundaries, add annotations update API, and failure-injection tests.                |
| 6     | Close release gates           | Fix lint; run Android density matrix; race Save/Back/Undo; rotate/resize; stress hundreds of strokes; verify memory and temp-file stability. |

### Smallest credible ship scope

If schedule pressure prevents the full schema redesign, the minimum acceptable patch still needs non-coalescing Android results, dp/px normalization, removal of all continuous PNG exports, a locked and atomic Save snapshot, explicit history reset, propagated errors, complete file cleanup, and Android stress testing. Cosmetic cleanup alone will not make this implementation safe.

### Remaining after this implementer pass

1. **Native rebuild + device QA** — required before ship (density matrix, Save/Back/Undo races, rotation/scaling, memory, and temp-file stability).
2. **Default/Type live canvases** — skipped by request (ADR-0008).
3. **Save exits Scribble** — skipped by request (deliberate UX).
4. **Failure-injection tests** — still absent because the repository has no automated test harness; cleanup ownership and rollback paths were hardened directly.
